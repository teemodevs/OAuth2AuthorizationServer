server:
  port: 8082
  servlet:
    context-path: /auth
spring:
  jpa:
    # Driver Class = org.h2.Driver
    # JDBC URL = jdbc:h2:mem:testdb
    database-platform: H2
    show-sql: TRUE
    hibernate:
      ddl-auto: 'create-drop'

spring.h2.console:
  enabled: true
  path: /h2-console